<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.example.demo.notice.NoticeMapper">
	<resultMap type="com.example.demo.notice.NoticeDTO" id="notice_demo">
		<result column="write_date" property="writeDate"/>
		<result column="file_name" property="fileName"/>
	</resultMap>
	
	<select id="noticeform" resultMap="notice_demo">
		SELECT B.* FROM (SELECT ROWNUM as r, A.* FROM (SELECT notice_demo.* FROM notice_demo ORDER BY no DESC)A )B WHERE B.r BETWEEN #{begin} AND #{end}
	</select>
	
	<select id="totalCount">
		SELECT count(no) FROM notice_demo
	</select>

	<insert id="noticeWriteProc">
		<selectKey order="BEFORE" keyProperty="no" resultType="int">
			SELECT NVL(max(no), 0)+1 FROM notice_demo
		</selectKey>
		INSERT INTO Notice_bowfun
		VALUES(notice_demo_seq.nextval, #{title}, #{content}, #{id}, sysdate, 0, #{fileName})
	</insert>
	
	<select id="noticecontent" resultMap="notice_demo">
		SELECT * FROM notice_demo WHERE no=#{n}
	</select>
	
	<update id="incrementViews">
	    UPDATE notice_demo SET views = views + 1 WHERE no=#{n}
	</update>
	
	<select id="noticeDownload">
		SELECT file_name FROM notice_demo WHERE no=#{n}
	</select>
	
	<update id="noticeModifyProc">
		UPDATE notice_demo SET title=#{title}, content=#{content} WHERE no=#{no}
	</update>
	
<delete id="noticeDeleteProc">
    DELETE FROM notice_demo WHERE no=#{n}
</delete>
</mapper>





